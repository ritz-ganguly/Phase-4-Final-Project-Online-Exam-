import { FilterResultByUserNamePipe } from './filter-result-by-user-name.pipe';

describe('FilterResultByUserNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterResultByUserNamePipe();
    expect(pipe).toBeTruthy();
  });
});
